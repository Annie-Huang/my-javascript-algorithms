/*
  count = Math.floor(N/2)

  if N is evenNumber: use mod to decide.
    arr = []
    for (let i = 1; i <= count; i++) {
        arr.push(i);
        arr.push(-i);
    }
  else
    arr = [0]
    for (let i = 1; i <= count; i++) {
        arr.push(i);
        arr.push(-i);
    }

   return arr
*/
