/*
  1.create oddArr = []; evenArr = [];
    for (let i = 0; i < N; i++) {
      if (Math.abs(A[i]) % 2 == 0) {
        evenArr.push(A[i]);
      } else {
        oddArr.push(A[i])
      }
    }

  2. return evenArr.length == oddArr.length

 */
