/*
  newStr = str..split('').reverse().join('')

  for (let i = 0; i < N; i++) {
    compare each position,
    if different return No
    if ? && value, ? replace with value;
    if value && ?, ? replace with value;
    if ? && ?, ? replace with a
  }

 */
