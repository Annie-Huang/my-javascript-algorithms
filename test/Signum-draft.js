/*
    1. create a obj of count, loop through the array,
       if A[i] == 0, return 0;
       if A[i] > 0, do nothing;
       if A[i] < 0, add to count:
       {
          -1: N
       }
       return N % 2 == 1 ? -1 : 1
 */
